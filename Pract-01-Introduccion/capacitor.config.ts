import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Pract-01-Introduccion',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
