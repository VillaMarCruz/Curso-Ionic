import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Pract-02-Introduccion-Modulos-Componentes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
